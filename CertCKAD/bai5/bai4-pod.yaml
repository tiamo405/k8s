apiVersion: v1
kind: Pod
metadata:
  labels:
    run: configmap-pod
  name: configmap-pod
spec:
  containers:
  - image: busybox
    name: configmap-pod
    command: ["/bin/sh"]
    args: ["-c", "printenv && sleep 3600"]
    envFrom:
      - configMapRef:
          name: app-config
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
