apiVersion: v1
kind: Pod
metadata:
  name: hostpath-pod
spec:
  containers:
  - name: test
    image: busybox
    command: ["sleep","3600"]
    volumeMounts:
    - mountPath: /data
      name: host-vol

  volumes:
  - name: host-vol
    hostPath:
      path: /tmp/data
      type: DirectoryOrCreate